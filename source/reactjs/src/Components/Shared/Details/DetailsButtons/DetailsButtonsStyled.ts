import { CSSProperties } from 'react';
// all the buttons (close, maximize, refresh)
export const DetailActionIcon:CSSProperties = {
    float: 'right',
    marginRight: '8px',
    cursor: 'pointer',
    fontSize: '12pt'
};

// all buttons (back, close, maximize, refresh)
export const tooltipCalloutProps = { gapSpace: 0 };

// all buttons (back, close, maximize, refresh)
export const tooltipHostContainer = {
    root: {
        display: 'inline-block',
        float: 'right'
    }
};
